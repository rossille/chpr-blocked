machine:
  node:
    version: 4.0.0
general:
  artifacts:
    - coverage
dependencies:
  pre:
    - npm install -g codecov
test:
  override:
    - npm test
  post:
    - codecov -t $CODECOV_TOKEN || echo 'Sending coverage failed'
