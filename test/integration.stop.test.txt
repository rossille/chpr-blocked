'use strict';

const chprBlocked = require('../index').stop();

const blockProcess = require('./blockProcess');

setTimeout(cb => {
    blockProcess(80);
    setTimeout(() => process.exit(), 100);
});
